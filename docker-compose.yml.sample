# 这是一个示例文件，展示如何自定义 docker-compose 配置
# 复制此文件为 docker-compose.override.yml 来自定义配置
# docker-compose 会自动合并 override 文件中的配置

version: '3.8'

services:
  tonglema:
    ports:
      - "3000:80"  # 修改端口映射
    # environment:
    #   - ENV_VAR=value
    # volumes:
    #   - ./custom-nginx.conf:/etc/nginx/conf.d/default.conf

